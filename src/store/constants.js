// Store all string values here (e.g. action strings, toast message strings, etc.)

export const actions = {
  SET_CHANNELS: 'SET_CHANNELS',
  SEND_FORM: 'SEND_FORM',
  SET_POST_SUCCESS: 'SET_POST_SUCCESS'
}
